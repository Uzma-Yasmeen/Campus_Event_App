/**
 * Where the web client looks for the API.
 *
 * This is the only line to change when deploying. Point it at your API and
 * keep the trailing "/api" - for example:
 *
 *   window.CAMPUS_API_BASE = 'https://campus-events-api.onrender.com/api';
 */
window.CAMPUS_API_BASE = 'http://localhost:5000/api';
